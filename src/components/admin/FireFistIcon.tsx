import React from 'react'

/**
 * FireFist CRM Icon component for Payload admin panel
 * Used in mobile view, favicon replacement, etc.
 */
export const FireFistIcon: React.FC = () => {
  return (
    <div
      style={{
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
        width: '32px',
        height: '32px',
        fontSize: '1.5rem',
        lineHeight: 1,
      }}
    >
      🔥
    </div>
  )
}
